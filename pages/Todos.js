import React from "react";
import styleApp from "../style/styleApp";
import { MainContainer, Header, HeaderTitle } from "../style/styleTodos";

const Todos = () => {
  return (
    <MainContainer>
      <Header>
        <HeaderTitle>To Do List</HeaderTitle>
      </Header>
    </MainContainer>
  );
};

export default Todos;
